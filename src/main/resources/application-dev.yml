spring:
  datasource:
    url: jdbc:h2:file:./data/ticketmanagement-dev;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: true
  ai:
    vectorstore:
      elasticsearch:
        initialize-schema: true
